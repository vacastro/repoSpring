package UsuarioService;

import com.practica1.demobp.model.Usuario;

public class UserService {
	/*
	private Boolean validarPass (Usuario unUsuario) throws Exception{
		if (!unUsuario.getContrasenia().equals(unUsuario.getConfirmContrasenia())) 
		{throw new Exception("las contraseñs no son iguales!");
	}
		return true;

}
*/
	
}